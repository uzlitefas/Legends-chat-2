import type { Metadata } from "next";
import { Geist, Geist_Mono, Inter } from "next/font/google";
import "./globals.css";
import { cn } from "@/lib/utils";
import { ChildProps } from "@/types/props";

const inter = Inter({ subsets: ['latin'], variable: '--font-sans' });

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Legends chat",
  description: "Legends chat is a modern and secure messaging platform designed for fast and easy communication. It includes all the essential chat features such as private and group messaging, media sharing, and a customizable interface. The platform works smoothly across different devices and provides a safe, flexible, and user-friendly chatting experience.",
};

export default function RootLayout({ children }: ChildProps) {
  return (
    <html
      lang="en"
      className={cn("h-full", "antialiased", geistSans.variable, geistMono.variable, "font-sans", inter.variable)}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}